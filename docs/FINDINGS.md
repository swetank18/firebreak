# FINDINGS

Append-only. Dated. Signed with your lane letter.

| Date | Lane | Entry |
|---|---|---|
| 2026-09-09 | A | **Realism gate FAILED on first run. Two distinct causes, both real bugs, neither a tuning problem.** Reported here before fixing, per AGENTS.md rule 8. |
| 2026-09-09 | A | **Cause 1 — my power-law fitter was broken.** The x_min search scanned all unique cascade sizes and selected x_min=128, deep inside the finite-size cutoff (city has 1063 nodes, max cascade 213). It fitted the cutoff as though it were the scaling regime and returned alpha=17.7. Restricting x_min to a sensible range recovers **alpha = 1.51 at x_min=2, rising to 2.09 at x_min=20 — squarely inside the [1.3, 2.5] target.** The heavy tail was there all along; the estimator was reading the wrong part of the distribution. |
| 2026-09-09 | A | **Cause 2 — a 120-member load-redistribution clique.** KS was genuinely rejecting (0.13-0.20 against a 0.011 critical value) because the CCDF has a bump around size 50: counts fall 104 at size 20, 38 at size 30, then rise to 135 at size 50. Traced it to flow topology, not dependency topology — dependency fan-out is healthy (max 6 dependents). All 120 telecom towers had `telecom.bh_01` as their sole flow parent, making every tower a sibling of every other, so a single 120-node Motter-Lai redistribution group either barely moves or avalanches wholesale. That produces a *characteristic* cascade size, which is precisely the "threshold not a process" failure mode `docs/02-CITY-SPEC.md` warns is the subtle one. Real telecom has regional backhaul, not one national node. |
| 2026-09-09 | A | **Also observed:** 52% of cascades are size 1 — failures that propagate to nothing. That is correct and expected, and it is exactly the near-miss population Lane B will mine. 296 nodes have a single flow parent (no redundancy), which is realistic for distribution networks but worth knowing when interpreting arm A2 centrality results. |
| 2026-09-09 | A | **Cause 3, the real one — the system is SUBCRITICAL, and I nearly missed it by blaming my own gate.** After fixing the fitter and the telecom clique the gate still failed, so I tested whether the criterion itself was too strict (KS against an asymptotic critical value at n=15k rejects almost any imperfect fit — a known error that Clauset et al. exists to address). It is not too strict. A Vuong likelihood-ratio test of power-law against exponential favours **exponential at every x_min, decisively (p ~ 0)**. The tail is heavier than normal — max is 9.4 sd above the mean, P(size>=50)=0.083 — but it decays exponentially. In branching-process terms that is subcritical: rho(G) < 1, cascades die out geometrically. 52% of cascades are size 1 and the median is 1, which is consistent. A power law only emerges near criticality. Recorded before any parameter was touched. |
| 2026-09-09 | A | **Meshing the power layer moves toward criticality but does not reach it.** Real urban distribution is meshed with normally-open tie switches, so feeders were cross-connected and substations ringed. Max cascade 207 -> 333 and LR -1823 -> -1387 (less exponential), but the verdict stays exponential. Adopted anyway because it is the more realistic topology, not because of the gate. |
| 2026-09-09 | A | **DECISION: stop pushing, and report the negative result.** Further parameter movement toward a power law would be fitting the simulator to the gate rather than to reality, which is the failure AGENTS.md rule 8 exists to prevent. The power-law claim in the literature (Dobson/Carreras; restated in PI-GN-JODE) is established for **transmission-grid blackouts**. Our system is a multi-sector urban lifeline network, where that has not been established and may well be false — different mechanism, different coupling, mostly non-electrical. Asserting it would have been an unexamined transfer from one domain to another. |
| 2026-09-09 | A | **The gate was replaced with what it actually needs to establish**, and the original power-law test is retained and REPORTED AS FAILING rather than deleted. What the project actually requires of the simulator: cascade sizes span orders of magnitude (1 to 333), the tail is far heavier than thin-tailed (max is 9.4 sd above the mean, P(size>=50)=8%), it is neither all-tiny nor all-collapse (median 2, 52% size 1), and it is not bimodal (20% mid-range mass). Those are the properties that make prediction possible and discrimination measurable. Whether the tail is exactly Pareto is not one of them. |
| 2026-09-09 | B | **The miner spec modelled one of the engine's two propagation mechanisms.** `docs/00-SHARED-CONTRACTS.md` §3 defines a chain as `(a declared dependency edge exists) AND (0 < dt <= W)`. But the engine propagates two ways, and measuring the ground truth shows the split is **674 links (17%) via `depends_on` against 3,281 (83%) via flow siblings** — load redistribution is the dominant mechanism and the miner could not see any of it. Ceiling on recall was 17% before a line of estimation was written. Fix: candidate parents are now both dependents and flow-siblings, and the relation type is carried on the link so Lane F can report recovery separately per mechanism. |
| 2026-09-09 | B | **`0 < dt` silently discarded half the corpus.** Median ground-truth propagation delay is **0 s** — load redistribution resolves within a tick — so a strict inequality threw away every same-tick link. Changed to `0 <= dt <= W` with event sequence order as the tiebreak for simultaneous events. That is observable information (the order events arrived in), not leakage. Delay distribution: median 0 s, p90 300 s, p99 12,900 s, max 18,600 s; **98.3% fall inside W = 10,800 s**, so the window itself is well chosen. |
| 2026-09-09 | F | **Chain recovery: precision 0.827, recall 0.875, F1 0.850 over 100 scenarios.** The miner reconstructs true parentage well without ever seeing `Event.cause`. On the thesis question — are near-misses recoverable from observable operational data — the answer is **yes**. |
| 2026-09-09 | F | **But random-among-admissible scores 0.678, so the timing rule is worth only 1.2x.** Most children have one or two admissible parents, so the topology alone nearly determines the attribution. This is a real finding and it is the same shape as H4: **the dependency and flow topology does most of the causal attribution work; the nearest-in-time heuristic adds little.** Reported rather than buried, because it says something useful — you do not need clever attribution, you need the topology. It is also a property of infrastructure networks specifically (sparse, hierarchical, highly constraining) and would not transfer to a dense network. |
| 2026-09-09 | F | **Precision splits sharply by mechanism: `depends_on` 0.991, `flow_sibling` 0.786.** Dependency edges are almost perfectly recoverable; load-redistribution links are where the error lives, and they are 80% of all links. That localises the residual error to one mechanism rather than leaving it diffuse — the same move as DIVAS pinning its coverage shortfall to a single documented constant. |
| 2026-09-09 | F | **The verdict criterion `P > 2 x chance` was wrong and is being replaced, not relaxed to fit.** It demanded the timing heuristic beat topology by 2x, which is not what the thesis claims. The thesis claims chains are recoverable from observable data; the test of that is absolute precision and recall, and 0.83/0.88 answers it. Lift over chance stays as a reported secondary diagnostic with the interpretation above. Open question for Lane B: does the 17% attribution error bias the kernel, or is it unbiased noise that averages out over thousands of links? Measurable, and it should be measured. |
| 2026-09-09 | B | **Kernel recovery on synthetic ground truth: alpha Spearman +0.896, Pearson +0.870, scale bias 1.05x.** The counting estimator recovers both the ordering and the scale of a known Hawkes kernel. This is the foundation test — it was written before the fitter, and had it failed nothing downstream would have meant anything. Median n_obs = 77 over 20 well-observed edges. |
| 2026-09-09 | B | **Delay is systematically UNDER-estimated: true median 6,947 s against fitted 4,022 s (Spearman +0.767).** This is truncation bias, and it is expected rather than surprising: the attribution window W = 10,800 s censors long links, so the estimator only ever sees the faster half of a slow edge's delay distribution. Ordering survives; scale does not. Consequence for Lane C: **intervention deadlines computed from 1/beta will be optimistic** — the system will think its window closes sooner than it does. Safe direction to be wrong in, but it must be stated, not discovered. Correctable later with a censored-likelihood estimator (the MLE upgrade), which is now a stronger reason to build it than "MLE is better". |
| 2026-09-09 | B | **rho under-estimated: 0.638 fitted against 0.750 true**, same cause — dropped long links lose branching mass. Direction matters: we under-estimate criticality, so the system is conservative about calling something supercritical rather than alarmist. |
| 2026-09-09 | B | **Only 20 of 42 candidate edges received any attributed link.** The rest never fired in the corpus. This is the thin-edge problem the contract's `is_thin` flag exists for, and it is what the deployment story has to answer: an edge with no history has no kernel, so the decision layer must fall back to the `nominal_delay_s` engineering prior and widen its interval. |
| 2026-09-09 | B/C | **H1 first run: branching ratio scored AUC 0.500 — exactly chance. Cause was an integration bug, not the method.** Flow-sibling relations have no `Edge` in the graph (siblings are peers under a shared parent, not connected directly), so the estimator invented synthetic keys `"src~dst"` while `BranchingRatio` looked up `"src->flow_parent"`. The keys never matched, so **80% of the propagation mass — every load-redistribution edge — was silently dropped from n_i**. No exception, no warning, just a number with the signal removed. |
| 2026-09-09 | B/C | **This is exactly the class of bug the contract freeze was meant to prevent, and it slipped because sibling edge identity is not in the contract.** `docs/00-SHARED-CONTRACTS.md` §1 defines `Edge.id` for graph edges only; the sibling relation is derived, so two lanes derived it differently. Fix: one canonical `sib:{src}->{dst}` key with a single shared helper, plus a test asserting every kernel key resolves in the consumer. Contract amended. |
| 2026-09-09 | F | **Multi-hop cascade reach (depth 6) scores 0.623 against one-step branching ratio 0.625 — no improvement.** Built because the label asks a 6-hop question while n_i answers a 1-hop one, so the shapes did not match. They turn out to be equivalent predictors here. The signal in this network is local: knowing the immediate neighbourhood is as good as propagating six generations. Reported; the simpler statistic wins and n_i stays the headline number. |
| 2026-09-09 | F | **METHOD VIOLATION, self-reported: the test seeds were run more than once.** `docs/03-EVALUATION.md` requires the test set be touched exactly once. It was scored three times while chasing the key-mismatch and spectral-radius bugs. Those runs were debugging integration faults, not selecting a model, so the leak is weak — but it is a leak. Correction: the reported H1 number is regenerated on a **fresh seed range never previously scored**, and the debugging runs are disclosed rather than quietly dropped. |
| 2026-09-09 | F | **H1 FINAL, fresh seeds (90000+), never previously scored.** Pre-registered: anomaly AUC in [0.45, 0.60], ours >= 0.80. Measured: **anomaly 0.463 — confirmed**, ours **0.621 — short of 0.80**. Half the hypothesis holds and it is the half the pitch rests on: ranking alerts by how loud they are predicts catastrophe no better than a coin, and slightly worse. Our own target was optimistic and we say so. |
| 2026-09-09 | F | **On the operational label, the gap is much larger: 0.479 vs 0.708, delta +0.229.** Asking "does this reach the health layer" — what an operator acts on, and what the damage function measures — separates the two predictors far better than "does a 6-hop chain follow". The pre-registered label was a proxy for danger; the health label IS danger. Both reported, with the pre-registered one first, because choosing the flattering label after seeing results is exactly what pre-registration exists to prevent. |
| 2026-09-09 | C | **The deadline solver was computing nothing.** `CascadeRollout.run` accepted `skip_generations` and never used it, so every candidate was scored as if ordered immediately and the binary search compared identical rollouts. Every intervention came back with the same 3-minute deadline, which is what made it obvious. The deadline is the field that turns a prediction into an instruction, so a silently inert one is the worst kind of bug here — it looks like an answer. Fixed by switching the propagation matrix mid-rollout at generation k. |
| 2026-09-09 | C | **Rollout performance: 2000 rollouts x 1069 nodes in 176 ms**, against a 2 s budget. Batching the branching process as one sparse matmul per generation (`F @ A`, R x n at a time) rather than a Python loop per rollout is what buys it. The live demo does not need precomputation after all. |
| 2026-09-09 | A/F | **Interventions shifted the RNG stream, which would have made the whole ablation measure noise.** The engine drew from one sequential generator, so protecting a single node changed every subsequent draw for every other node. First test: protecting one node out of 1069 changed total failures by -21 — divergence, not effect. Paired statistics across arms require paired randomness, and without it arm-to-arm differences are dominated by stream drift rather than by the intervention. Fixed with a precomputed (tick x node) random field indexed by position, so a node's draw is identical across arms regardless of what any other node did. This is the kind of fault that produces a confident, entirely fictional results table. |
| 2026-09-09 | A/F | **The realism gate checked cascade sizes and missed scenario saturation.** In a monsoon run, **986 of 1069 assets fail and 6.35M of 7.13M people are affected** — 89% of the city. Every scenario is total collapse, so no 5-node intervention can move the outcome and the ablation measures nothing. The gate's "not collapse" check tested the *cascade size* distribution, which passed, and never tested what fraction of the CITY ends up down per scenario. Those are different questions and only the second one decides whether intervention is measurable. Prediction only matters where the outcome is contingent. |
| 2026-09-09 | A/F | **The oracle was misdefined and read BELOW arm A2** (prevented 1,788 against A2's 1,324,991), which is impossible for a ceiling. It ranked nodes by blame count — how many downstream failures they were credited with — but blame count is not damage. A node causing fifty small failures scores above one causing a hospital outage. Replaced with a greedy oracle evaluated by actually re-running the engine over a candidate pool. |
| 2026-09-09 | A | **Tick size was changing the physics, and it had been changing it all along.** Exogenous ignition was `p = 0.0016 * stress * susceptibility` applied once per tick, so a discretisation choice set the ignition intensity: the same seed and scenario produced **611 exogenous ignitions at tick_s=60 against 177 at tick_s=600**. Every run in the project used tick_s=300 so nothing was inconsistent between results, but the number was arbitrary and nobody had chosen it. Replaced with a rate per second, `p = 1 - exp(-lambda * stress * susceptibility * tick_s)`. Ignitions now hold at 36.5 against 34.5 across the same 10x range in tick size. Residual sensitivity remains in cascade *speed* (a cascade advances one generation per tick either way), which is inherent to the discretisation; all evaluation is fixed at tick_s=300 and that is stated rather than left implicit. |
| 2026-09-09 | A | **The flood had no footprint.** `monsoon_flood` gave every asset in the city at least 35% of its exposure for the whole event, so a flood behaved like a city-wide degradation rather than an inundation of a region. Combined with the per-tick ignition rate this is what produced the saturation: 92% of assets down with a seed-to-seed spread of 0.90-0.94, an outcome no intervention could move. Rebuilt as a water level that rises, peaks and recedes, where an asset is stressed only while the level exceeds its elevation. Assets on high ground are now never touched. |
| 2026-09-09 | A | **Fixing the flood exposed that the cascade could not leave the road layer.** With a footprint and a calibrated rate, monsoon runs settled at 107 of 113 failures being roads — and roads serve no population, so the damage function read almost zero. Cross-layer propagation was real but rare, because ignitions were dominated by roads (700 of 1069 assets) and only power feeders are upstream of water, telecom and health. The contingent regime is the one where power assets in the inundated zone fail at a meaningful rate; measured across a rate sweep, that is where population affected spans 15%-88% by seed instead of sitting at a constant. |
| 2026-09-09 | A | **The other four hazards were silently calibrated against the old per-tick model, and all of them saturated once it changed.** heatwave, cyber and equipment_age reached 0.86-0.91 of population affected. equipment_age is the near-miss corpus generator and MUST produce mostly small cascades, so a saturating background rate would have quietly corrupted the training corpus for every kernel in the project. Per-hazard intensity is now explicit and relative to the flood, which is the only one with a real anchor (December 2015). The other four are a judgement call calibrated to a stated operational profile, and that is recorded in DECISIONS.md and LIMITATIONS.md rather than buried in an exposure table. |
| 2026-09-09 | F | **The realism gate now checks contingency, and this is the check whose absence cost an entire ablation.** The size distribution passed cleanly the whole time the city was collapsing in every run, because cascade size and scenario outcome are different questions. Only the second decides whether an intervention is measurable. All five hazards now pass: monsoon 74% of population affected at the median but spanning 15%-88%, equipment_age 2%, and none ~0%. |
| 2026-09-09 | F | **Damage was a flat constant and the engine's restoration model was therefore invisible.** `person_hours` was `population * 4.0` for anything that failed at all, so an asset down for twenty minutes cost exactly what one down for three days cost — and an intervention that shortens an outage scored nothing for it. Replaced with real outage hours from paired failed/restored events. This matters more than it sounds: restoration is slowed by the access penalty while roads are flooded, so shortening outages is a large part of what a good intervention actually does here. |
| 2026-09-09 | C | **The decision layer was violating its own invariant, in the two places an import check cannot see.** `EXECUTION.md` section 5 requires that no executable line in `decision/` name a layer, precisely because a string comparison couples the engine just as tightly as an import. `rollout.py` had `n.layer == "health"` and `search.py` had `node.layer == "transport"`. The AST test that was supposed to catch this had never been written. Fixed by adding a domain-neutral `Node.protected_class` to the contract and deriving the rationale from structure (`serves no population, yet N assets depend on it`) rather than from knowing what a road is. The second sentence is truer than the one it replaced. |
| 2026-09-09 | D | **`site/` shadowed the stdlib `site` module** — the same class of fault as the `platform/` rename, and the regression test written for that one caught it. Renamed to `www/`. Worth noting the test earned its keep within a day of being written. |
| 2026-09-09 | F | **The pitch page claimed "numbers regenerate from eval/, none typed by hand" directly above a table of hand-typed constants**, several of which had already gone stale against the results in the repo. The claim is now true: `scripts/build_site.py` reads `eval/results/*.json` and fails the build on any unfilled placeholder. |
| 2026-09-09 | C/F | **The full-Firebreak arm A4 came last, and the cause was that its chosen action was thrown away before evaluation.** With the saturation fixed and the oracle honest, the ablation read A2 10.5% and A3 9.9% but **A4 0.1%** — the arm the whole project is named after, below every non-trivial baseline. The search was ranking by benefit per unit COST, `isolate` is the cheapest action in `contracts/costs.json`, and `A.candidates` explicitly proposes isolating every live seed. So four of A4's five picks were `isolate` on assets that had **already failed** by the decision time. The evaluation then discarded the action kind, kept the target, and applied generic protection — to a dead asset, which does nothing at all. Two separate faults compounding: an action space wider than the environment can represent, and a ranking rule that pushed the search straight into the unrepresentable part of it. |
| 2026-09-09 | C/F | **The fix is to make every arm choose five nodes to protect and nothing else**, so the arms differ only in HOW they choose. A4's search now runs over the actions the engine can actually apply (`harden`, `preposition`), is forbidden from spending budget on assets already down, and is ranked by raw damage prevented rather than per unit cost — the budget in this ablation is a count of interventions, so cost-normalisation is a different mechanism and does not belong in this arm. The same restriction applies to the demo export, which had the identical bug and would have shown a Firebreak action that protected four already-failed assets. |
| 2026-09-09 | C | **Excluding already-failed assets nearly starved the search.** With 157 of 1069 assets already down at the decision point, the reach hint's top-40 left two candidates, both on the same node — so the arm could not even spend its budget of five, and would have been compared at a different budget from every other arm. The hint is widened and the ablation asks for the full ranking rather than only actions with a demonstrable positive benefit. The product still declines to recommend an action it cannot show a benefit for; an ablation arm has to spend its budget. |
| 2026-09-09 | D | **Determinism and paired randomness had no test at all**, despite `EXECUTION.md` section 10 requiring byte-identical replay and the entire ablation depending on pairing. Added, along with the frozen fixtures that `AGENTS.md` rule 3 promises and `fixtures/` did not contain. The pairing test protects a structurally inert node — nothing depends on it, it has no flow-peer to shed load onto — and asserts every OTHER node's failure time is unchanged to the bit. Under the old sequential generator it would not have been. |
| 2026-09-09 | B/F | **Every kernel in the project was fitted on a corpus that mixed scenarios together, and the leakage test that would have caught it had never been written.** `counting.fit` takes a flat event list, and every caller built one by concatenating scenarios: `[e for s in train for e in redact(s.events)]`. Two independent faults follow. **(1) Event ids collided.** The engine numbered events `e000001` per run, so pooling six scenarios collided **8,263 of 12,877 ids**; `fit` keys its one-parent-per-child map by child event id, so links from one scenario silently overwrote links from another. **(2) Chains straddled scenarios.** Every scenario runs over the same 0..horizon clock, so concatenating interleaves unrelated runs completely in time, and the `0 <= dt <= W` window happily attributes a failure in one scenario to a failure in another. Measured on six scenarios: **36,903 links pooled against 16,006 mined properly — 2.31x, and the entire surplus is phantom.** |
| 2026-09-09 | B/F | **This is exactly assertion 5 of the leakage spec** — 'scenario-level, not event-level splitting; chains from the same scenario never straddle a boundary… this is the leak that looks like a great result' — written in `docs/03-EVALUATION.md` and never implemented. `service/redact.py` also claimed in its own docstring that an AST test enforced the `cause` boundary; no such test existed either. The five specified assertions are now written. Fix: event ids are prefixed with the scenario id, and `fit` takes a list of per-scenario corpora rather than one flat list, mining each independently and pooling the links. A flat list is still accepted and is correct only when it really is one scenario. |
| 2026-09-09 | B/F | **Note what this was and was not.** No ground truth leaked — `cause` and unobserved events were stripped correctly the whole time. It was a corpus-construction error, and it inflated the evidence supporting every edge (median n_obs, and therefore the thin-edge flag) as well as corrupting alpha. All results are regenerated against the fix; the earlier ones are not comparable and were not kept. |
| 2026-09-09 | F | **The oracle's candidate pool contained assets that were already down, which biases the ceiling in the flattering direction.** Protection lands at the decision time and cannot resurrect anything that failed before it, so those candidates are no-ops occupying slots in a 30-candidate pool. A ceiling built partly from no-ops reads LOWER than the true achievable one, and every arm's '% of oracle' therefore reads HIGHER. Caught by reading the code rather than by a failing test, and fixed before the numbers were reported: the pool is now restricted to assets whose first failure is after the decision time — the ones an intervention could actually save. The oracle exists to be unflattering, so an error that makes it easier to look good against is the one to be least comfortable with. |
| 2026-09-10 | F | **THE ABLATION HAD NO CHANCE BASELINE, and with one it says something very different.** Protecting five assets chosen AT RANDOM prevents damage too — measured over 42 random draws across three scenarios, mean +23,947 with **sd 176,485**, ranging from -776,738 to +658,763 on a single scenario. Against that noise floor, the seven-arm table's point estimates were unreadable: A3's +0.4% and A4's -0.1% are indistinguishable from a random pick, and A3's -726,878 on one scenario sits inside the random range. Only A2 (+978,899) and the oracle stood clear. A table of point estimates with no interval presented 0.4% as an effect. Arm **AR — random 5 assets** is now a first-class arm, every arm carries a paired bootstrap CI over scenarios, and the reported column is **vs chance**, not vs doing nothing. |
| 2026-09-10 | F | **`eval/h1.py` printed 'H1 HOLDS' directly above a measured 0.766 against a pre-registered 0.80.** The pass criterion in the script was `0.40 <= anomaly <= 0.62 and ours >= 0.70` — a looser bar than the pre-registration, invented after the fact and never written down as a change. A verdict scored against a criterion the pre-registration does not contain is not a pre-registered verdict; it is the exact failure pre-registration exists to prevent, committed by the script that reports on it. Now judged against `[0.45, 0.60]` and `>= 0.80`, both halves reported separately. |
| 2026-09-10 | F | **The H2 declared-matrix baseline was capped by its own calibration grid, not calibrated.** The uniform alpha was chosen by minimising training RMSE over `[0.05 … 1.0]`, and it selected **1.0 — the largest value on offer — with RMSE still falling monotonically across the whole grid.** So the baseline was held down by where the grid stopped, and any margin we reported over it would have been partly our own doing. Grid extended to 8.0, with an explicit warning if the choice ever lands on an edge again. |
| 2026-09-10 | C | **The rollout implemented only half of the Hawkes model the project is built on, and that is why the decision layer could not choose an intervention.** The model we cite is `lambda_j(t) = mu_j + sum_i sum_k alpha_ij beta_ij exp(-beta_ij (t - t_k))`. `counting.fit` estimates `mu` and writes it into the `Kernel` contract; `decision/rollout.py` never read it. With only the excitation term, **nothing can fail unless an already-failed parent pushes it** — and over the remaining 54 hours of a 72-hour monsoon most further failures come from the hazard still running, not from branching off what has already failed. |
| 2026-09-10 | C | **It was invisible until H2 reported the predicted mean next to the actual one.** Both H2 arms scored RMSE ~302 against a mean actual cascade volume of 265, which reads like two comparable predictors — until the diagnostic showed the learned kernel predicting **3.1 assets** and the declared matrix **2.0**, against an actual **265**. RMSE ~= the actual mean because both were predicting approximately nothing, and the +0.4% margin between them was a comparison of two null models. A ratio between two broken numbers looks exactly like a result. |
| 2026-09-10 | C | **Second, compounding mismatch: the rollout's time span.** A generation is one propagation hop, median ~900 s, and it ran 12 of them — about 3 hours of a 3-day event, while being asked how much fails before the clock stops. Fixed together: `run()` takes the remaining horizon, spans it in up to 40 blocks, and adds `mu * dt` per block. On a held-out scenario the prediction moves from **8.2 to 172.7 against an actual 164**. 2,000 rollouts over the full horizon take 1,181 ms, inside the 2 s budget. |
| 2026-09-10 | C | **This is the mechanism behind arm A4 and it was not a tuning problem.** A4 ranked interventions by damage prevented under a model that under-predicted the thing it was ranking by two orders of magnitude, so its ordering carried no information about the engine's actual behaviour — which is exactly what the ablation measured when A4 came out indistinguishable from protecting five assets at random. The approximation that blocks introduce (multi-hop propagation compressed inside one block) is stated in LIMITATIONS rather than left implicit. |
