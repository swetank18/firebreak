[pytest]
testpaths = service/tests
# This machine has ROS on the system path, and its pytest plugins fail to import
# (launch_testing -> yaml). Nothing here uses them, and an unrelated plugin
# breaking collection is not a signal about this repo.
addopts = -p no:cacheprovider
